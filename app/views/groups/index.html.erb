<div id="description">
   <h3>All the groups</h3>
   <% @groups.each do |group| %>
      <div class="group">
         <div>
            <h3><%= link_to group.name, group %></h3>
         </div>
         <div>
            <p>
            <% if group.guests.empty? %>
               There are no guests in the <%= group.name %> group yet.
            <% elsif group.guests.size == 1 %>
               The only guest in the <%= group.name %> group is <%= group.guests.map{|g|g.name}.to_sentence %>.
            <% else %>
               The guests in the <%= group.name %> group are <%= group.guests.map{|g|g.name}.to_sentence %>.
            <% end %>
            </p>
         </div>
      </div>
   <% end %>      
   <div class="add-group">
      <%= link_to image_tag("add_group.png"), new_group_path, class:"add" %>
   </div>
</div>
